{
  "name": "date",
  "plugin_type": "processor",
  "description": "The date processor adds a default timestamp to an event, parses timestamp fields, and converts timestamp information to the International Organization for Standardization (ISO) 8601 format.",
  "properties": {
    "match": {
      "type": "object",
      "description": "The date match configuration. This option cannot be defined at the same time as <code>from_time_received</code>. There is no default value.",
      "required": false,
      "properties": {
        "key": {
          "type": "string",
          "description": "Represents the event key against which to match patterns. Required if <code>match</code> is configured.",
          "required": true
        },
        "patterns": {
          "type": "array",
          "description": "A list of possible patterns that the timestamp value of the key can have. The patterns are based on a sequence of letters and symbols. The <code>patterns</code> support all the patterns listed in the Java <a href=\"https://docs.oracle.com/javase/8/docs/api/java/time/format/DateTimeFormatter.html\">DatetimeFormatter</a> reference. The timestamp value also supports <code>epoch_second</code>, <code>epoch_milli</code>, and <code>epoch_nano</code> values, which represent the timestamp as the number of seconds, milliseconds, and nanoseconds since the epoch. Epoch values always use the UTC time zone.",
          "required": true,
          "items": {
            "type": "string"
          }
        }
      }
    },
    "from_time_received": {
      "type": "boolean",
      "description": "When <code>true</code>, the timestamp from the event metadata, which is the time at which the source receives the event, is added to the event data. This option cannot be defined at the same time as <code>match</code>. Default is <code>false</code>.",
      "required": false
    },
    "date_when": {
      "type": "string",
      "description": "Specifies under what condition the <code>date</code> processor should perform matching. Default is no condition.",
      "required": false
    },
    "to_origination_metadata": {
      "type": "boolean",
      "description": "When <code>true</code>, the matched time is also added to the event's metadata as an instance of <code>Instant</code>. Default is <code>false</code>.",
      "required": false
    },
    "destination": {
      "type": "string",
      "description": "The field used to store the timestamp parsed by the date processor. Can be used with both <code>match</code> and <code>from_time_received</code>. Default is <code>@timestamp</code>.",
      "required": false
    },
    "output_format": {
      "type": "string",
      "description": "Determines the format of the timestamp added to an event. Default is <code>yyyy-MM-dd'T'HH:mm:ss.SSSXXX</code>.",
      "required": false
    },
    "source_timezone": {
      "type": "string",
      "description": "The time zone used to parse dates, including when the zone or offset cannot be extracted from the value. If the zone or offset are part of the value, then the time zone is ignored. A list of all the available time zones is contained in the <strong>TZ database name</strong> column of <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones#List\">the list of database time zones</a>.",
      "required": false
    },
    "destination_timezone": {
      "type": "string",
      "description": "The time zone used for storing the timestamp in the <code>destination</code> field. A list of all the available time zones is contained in the <strong>TZ database name</strong> column of <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones#List\">the list of database time zones</a>.",
      "required": false
    },
    "locale": {
      "type": "string",
      "description": "The location used for parsing dates. Commonly used for parsing month names (<code>MMM</code>). The value can contain language, country, or variant fields in IETF BCP 47, such as <code>en-US</code>, or a string representation of the <a href=\"https://docs.oracle.com/javase/8/docs/api/java/util/Locale.html\">locale</a> object, such as <code>en_US</code>. A full list of locale fields, including language, country, and variant, can be found in <a href=\"https://www.iana.org/assignments/language-subtag-registry/language-subtag-registry\">the language subtag registry</a>. Default is <code>Locale.ROOT</code>.",
      "required": false
    }
  }
}